from django.db import models
from django.conf import settings

# AI Generation Settings have been removed.
# All AI context is now provided through the Project Briefing field in SiteSettings (core app).
# This simplifies the architecture by consolidating all project/business context in one place.
